//? Return statement in function in js

function tenTimes(num) {
  const result = num * 10;
  return result;
} 

const tenTimesResult = tenTimes(8);
console.log(tenTimesResult);

function cutHalf(num) {
  const half = num / 2;
  return half;
}

const cutHalfResult = cutHalf(8);
console.log(cutHalfResult)